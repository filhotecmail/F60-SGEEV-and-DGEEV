# ====================
# F60-SGEEV-and-DGEEV Environment Configuration
# ====================

# Version and Build Info
VERSION=1.0.0
GIT_COMMIT=

# Directory Paths (customize as needed)
DATA_PATH=./data
OUTPUT_PATH=./output
LOGS_PATH=./logs

# Application Settings
APP_ENV=production
LOG_LEVEL=INFO
TZ=America/Sao_Paulo

# Fortran Compiler Settings
FORTRAN_RUNTIME=gfortran
FORTRAN_FLAGS=-O3 -march=native -ffast-math

# Development Settings
DEBUG=false
ENABLE_HOT_RELOAD=false
ENABLE_METRICS=true

# Testing Settings
TEST_MODE=false
COVERAGE_ENABLED=false

# Performance Settings
BENCHMARK_MODE=false
PERFORMANCE_MONITORING=false

# Security Settings (for production)
# GITHUB_TOKEN=your_github_token_here
# REGISTRY_URL=ghcr.io

# Docker Compose Profiles
# Uncomment to set default profile
# COMPOSE_PROFILES=dev
# COMPOSE_PROFILES=test
# COMPOSE_PROFILES=benchmark

# Resource Limits (optional override)
# MEMORY_LIMIT=512M
# CPU_LIMIT=1.0

# Network Configuration
# SUBNET=172.20.0.0/16
# GATEWAY=172.20.0.1

# ====================
# Usage Examples:
# ====================
# cp .env.example .env
# Edit .env with your specific values
# docker-compose up (uses production settings)
# docker-compose --profile dev up sgeev-dev
# docker-compose --profile test up sgeev-test